GLuint texture_id;

void InitLoadTextures() {
	texture_id = loadBMP(texture_name);
}

InitLoadTextures komt tussen InitMatrices en InitBuffers.

InitGlutGlew(argc, argv);
InitShaders();
InitMatrices();
InitLoadObjects();
InitLoadTextures();
InitBuffers();

Voor het renderen moet je nog wel glBindTexture doen, na het usen van program_id/shader_id en voordat je het renderen van je cube doet:

texturedShader.Use(); //Textures: http://www.opengltutorial.org/beginners-tutorials/tutorial-5-a-textured-cube/
	
glBindTexture(GL_TEXTURE_2D, texture_id);

cube.Render(camera.projection, camera.view);